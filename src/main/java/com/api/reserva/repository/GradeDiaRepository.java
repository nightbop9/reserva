package com.api.reserva.repository;

import com.api.reserva.model.GradeDiaModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GradeDiaRepository extends JpaRepository<GradeDiaModel, Long> {
}
